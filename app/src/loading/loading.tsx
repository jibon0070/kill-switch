import './loading.scss'

function Loading() {
    return (
        <div id={'loading'}>
            <div className="ring">
                Loading
            </div>
        </div>
    );
}

export default Loading;